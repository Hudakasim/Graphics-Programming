function setup() {
  createCanvas(400, 400);
}

function draw() {
  let a = color(220, 225);
  let b = color('red');
  let c = color(220);
  let d = color(225,0, 225);
  let e = color(255, 255, 0, 255);
  let f = color(0, 200, 10);
  colorMode(HSL); //Hue-Saturation-Lightness
  // background(f); you can used it either way 
  background(255, 0, 255);
}